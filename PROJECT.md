# 나의 대회 기록증 (My Race Certificate) — 프로젝트 문서

마라톤 · 철인3종 · 그란폰도 · 트레일러닝 대회의 **기록증을 보관하고, 기록·사진·날씨·몸 상태·코스까지 한곳에서 관리**하는 개인용 안드로이드 앱.

- 패키지: `com.yuchoi.racecert` · minSdk 26 · targetSdk 35 · compileSdk 36
- 스택: Kotlin + Jetpack Compose(Material3), 단일 Activity + 상태 기반 화면 전환
- 저장: 로컬 JSON(`files/records.json`) + 내부 이미지 폴더(`files/images/`) — 서버 없음
- 배포: GitHub Actions가 push마다 서명된 APK를 빌드해 Releases에 올리고, 앱이 스스로 업데이트

---

## 1. 설치와 업데이트

| 상황 | 방법 |
|---|---|
| 처음 설치 | [Releases](https://github.com/yuchoi-bb/My-Race-Certificate/releases)에서 최신 APK 다운로드 → 설치 |
| 이후 업데이트 | 앱 실행 시 새 버전 안내가 자동으로 뜸 → "지금 업데이트" |

- 버전 규칙: `versionName = 1.0.<GitHub Actions 빌드번호>`, `versionCode = 빌드번호`
- 업데이트 확인은 GitHub Releases **목록**에서 versionCode가 가장 큰 릴리스를 골라 비교
  (두 빌드가 동시에 배포돼 latest 포인터가 꼬여도 안전)

## 2. 화면 구성

하단 탭 3개 + 상세/편집 화면.

| 탭/화면 | 파일 | 내용 |
|---|---|---|
| 기록 (목록) | `ui/RecordListScreen.kt` | 예정 대회(D-day) + 연도별 기록 카드 목록 |
| 요약 · PB | `ui/SummaryScreen.kt` | 참가 통계, 개인 최고기록(PB) 카드, Strava/Drive 연동, 백업/복원 |
| 몸 상태 | `ui/BodyGraphScreen.kt` | 헬스커넥트 몸 상태 이력 그래프 |
| 기록 상세 | `ui/RecordDetailScreen.kt` | 모든 정보 + 사진 갤러리 + 코스 지도 |
| 기록 추가/수정 | `ui/AddEditRecordScreen.kt` | 섹션형 입력 폼 (가장 큰 화면) |
| 공유 가져오기 | `ui/ShareImportScreen.kt` | 갤러리에서 공유(Share)로 받은 사진을 대회에 붙임 |

## 3. 기능 상세

### 3.1 대회 기록 관리
- 등록/수정/삭제. 필수 항목은 **대회 이름 + 날짜**뿐, 나머지는 선택.
- 종목: 마라톤 / 철인3종 / 그란폰도 / **트레일러닝** / 기타.
- **미래 날짜로 저장하면 "예정된 대회"**: 목록 맨 위에 D-day 카드로 고정, 가장 가까운 1개만 펼치고 나머지는 접힘. 대회명으로 웹 이미지 검색(DuckDuckGo)해 대표 사진을 미리 붙일 수 있음.
- 대회 이름의 연도(2025 등)는 저장 시 자동 제거, 날짜는 2자리 연도(`yy.MM.dd`)로 표기.
- 저장 후 목록에서 **방금 저장한 카드 위치로 자동 스크롤 + 강조**.
- 뒤로가기 시 미저장 변경이 있으면 저장/무시 확인 다이얼로그.

### 3.2 기록 카드 (목록·PB 공용)
- 마라톤 거리별 파스텔 색: 풀=레드, 하프=그린, 10K=블루, 그 외=그레이.
- **대표 이미지**(세로 88×116)와 **흐린 배경 이미지**를 업로드 사진 중 **각각 따로 지정** 가능.
- 우측 상단에 날씨(아이콘, 줄바꿈 표기: ☁️기온 / 💧강수 / 💨바람 / 🕘시간대).
- 종목·거리별 개인 최고기록이면 **우측 하단 🏆 PB 배지**.

### 3.3 사진
- 기록당 **최대 20장** (`RaceRecord.MAX_IMAGES`).
- 가져오기 소스 3종: 포토 피커 / 다른 앱(기기 갤러리 등) / **대회 무렵 사진**(대회일 7일 전~당일, MediaStore 날짜 필터 그리드).
- 새로 추가한 배치는 **촬영 시각(EXIF→MediaStore)순 자동 정렬**, 기존 사진 순서는 유지.
- 사진별 컨트롤: 삭제, ◀▶ 순서 이동, 90° 회전, 탭하면 전체 미리보기, **대표/배경 지정 토글**.
- 갤러리 앱에서 **공유(Share)** 로 사진을 보내면 앱이 받아 같은 날짜 대회를 추천(EXIF 촬영일 기준)하거나 새 기록 생성.
- 상세 화면: 큰 메인 이미지 + 썸네일 스트립(탭하면 메인 교체).

### 3.4 OCR (기록증 자동 인식)
- ML Kit 온디바이스 한국어 OCR(`ocr/OcrEngine.kt`) — 네트워크 불필요.
- `ocr/CertificateParser.kt`가 대회명/날짜/기록/거리/종목/배번을 정규식으로 추출해 **빈 칸만** 채움 (사용자 입력을 덮지 않음).
- 디지털 폰트 오인식 보정(O→0, I→1), 완주시간 0~29시간 검증, 한국어/ISO/영문 날짜 형식 지원.
- 사진 추가 시 **새 사진만 증분 인식**해 기존 원문에 덧붙임(전체 재인식 방지). 수동 "다시 읽기"는 전체 재인식.
- 인식 후 필수 항목(이름·날짜·기록) 성공/누락 요약 다이얼로그.

### 3.5 날씨
- `net/WeatherService.kt` — Open-Meteo(무료·키 불필요). 과거는 archive API, 최근/미래는 forecast API.
- 장소 검색: Nominatim(OSM, 한국 지명에 강함) + Open-Meteo 지오코딩. 도로/철도 등은 제외하고 후보를 보여줘 사용자가 선택.
- **대회 시작 시간~완주 시간대의 시간별 날씨를 요약** (없으면 하루 전체). 형식: `장소 · ☁️ 7~24°C 💧0mm 💨15km/h 🕘07~11시`.
- 시작 시간 기본 08:00, 장소가 서울이면 07:30 자동.
- Strava에서 기록을 가져오면 **활동 시작 좌표 + 실제 시간대**로 날씨를 재계산해 덮어씀.

### 3.6 몸 상태 (Health Connect)
- `health/HealthConnectBody.kt` — 헬스커넥트에서 **체중·체지방률·제지방량·기초대사량** 읽기.
- InBody·가민커넥트·삼성헬스 등이 헬스커넥트로 **내보낸** 값을 읽는 구조 (각 앱에서 헬스커넥트 연동을 켜야 함).
- 기록 편집에서 "가져오기" 시 **대회 전~대회일까지** 범위에서 대회일에 가장 가까운 측정값 사용 (대회 후 측정값 제외). 측정일은 "대회 -N일" 형식으로 표시.
- 실패 지점을 **숫자 코드**로 안내: 1 미지원 / 2 헬스커넥트 설치 필요 / 3 권한 허용됨 / 4 권한 거부 / 5 데이터 없음 / 6 성공 / 8·9 오류.

### 3.7 몸 상태 그래프 탭
- 지표별 선 그래프: 체중 / 체지방률 / 제지방량 / 기초대사량 (있는 지표만 카드로).
- 기간 선택: **3개월 / 1년 / 3년 / 10년 / 전체** — 페이징으로 전체 이력 로드.
- 카드마다 현재값·최저/최고·측정 횟수·기간 라벨. 우측 상단 🔄 새로고침.
- 차트는 외부 라이브러리 없이 Canvas로 직접 그림.

### 3.8 Strava 연동
- `strava/` — OAuth2(Authorization Code). Client ID는 코드에, **Client Secret은 GitHub Secret(`STRAVASECRET`)** 으로 CI 빌드 때만 주입.
- 요약 탭에서 계정 연결/해제. 로그인은 브라우저/Strava 앱으로 열리고 `racecert://localhost` 딥링크로 복귀, 토큰은 자동 갱신.
- 기록 편집 → **"Strava에서 기록 가져오기"**:
  - 일반 종목: 그 날짜의 가장 긴 러닝 1건 → **완주시간·거리·시작시간을 덮어쓰고**, 상세 지표(종목·페이스·심박 평균/최대·케이던스·고도)와 코스 폴리라인 저장, 날씨 재계산.
  - **철인3종**: 그 날의 모든 활동(🏊🚴🏃) 목록을 띄워 **체크박스로 선택 → 합산** (총 시간 = 첫 시작~마지막 종료, 거리 = "1.5 / 40.0 / 10.0km", 종목별 요약 + 🏁 합계, **선택한 모든 경로**를 지도에 표시).
- 실패 코드: 1 Secret 미설정 / 2 로그인 필요 / 5 그 날짜 활동 없음 / 9 통신 오류.
- 참고: Strava는 체중 등 체성분을 제공하지 않음(몸 상태는 헬스커넥트 경로).

### 3.9 코스 지도
- `ui/RouteMap.kt` — 지도 SDK·API 키 없이 **OpenStreetMap 래스터 타일** 위에 경로를 그림.
- 경로 범위에 맞는 줌 자동 선택 → 필요한 타일만 다운로드(기기 캐시) → Web Mercator 투영으로 경로 오버레이.
- 철인3종은 여러 종목 경로를 **한 지도에 개별 선**으로 함께 표시.

### 3.10 개인 최고기록 (PB)
- `data/PbCalc.kt` — 목록의 배지와 요약 탭이 같은 계산을 공유.
- **마라톤은 사용자가 고른 세부종목(10K/하프/32K/풀코스)으로만 PB 판정** — 실제 거리 입력값은 기록용일 뿐 PB에 영향 없음(10.0/10/10.2 구분 불필요). 편집 화면에서 실제 거리 기반 기본값을 제안.
- 그 외 종목은 정규화한 거리(하프/풀코스/NNkm) 기준.
- 요약 탭의 PB 목록은 **기록 카드와 동일한 카드**로 표시되고 탭하면 상세로 이동.

### 3.11 요약 · PB 탭
- 참가 요약: 전체/올해 참가 횟수, PB 종목 수, 종목별 횟수, 올해 참가비 합계(기본+이벤트), 펼치면 **거리 카테고리 × 연도별** 세부 횟수.
- 참가비는 **기본 참가비 + 이벤트 추가금(증정품 등) + 이벤트 구성 메모**로 분리 입력, 총액 자동 계산.

### 3.12 백업 / 복원 / 동기화
- **수동 백업**: 기록(JSON)+사진을 zip 하나로 저장(SAF → Drive 등). 기록이 참조하는 이미지만 포함(고아 파일 제외). 기록이 0개면 백업을 막고, 성공 시 "기록 N개" 표시.
- **복원**: zip을 골라 복원. 실패 사유를 구체적으로 안내(파일 못 엶/zip 아님/기록 없음/IO 오류). 복원 데이터는 '가장 최신'으로 표시돼 Drive 동기화가 되돌려 덮어쓰지 않음. Zip Slip 방어 포함.
- **Google Drive 자동 동기화**: Google 로그인 → Drive appDataFolder에 백업 1개를 두고 **마지막 변경 우선(last-writer-wins)** 으로 여러 기기 동기화. 앱 시작·저장·삭제 시 자동 트리거. (Google Cloud Console에 패키지명+SHA-1 OAuth 등록 필요 — 완료됨)
- 이미지 경로는 파일명 기준으로 재매핑해 **다른 기기에서 복원해도 경로가 깨지지 않음**.

### 3.13 자체 업데이트 & CI/CD
- `.github/workflows/build-release.yml`: `main`/`claude/**` push마다 서명 APK 빌드 → `v1.0.<run_number>` 태그로 Releases 등록.
- `update/UpdateChecker.kt`: 릴리스 목록에서 최고 versionCode 선택. `update/UpdateInstaller.kt`: DownloadManager로 받고 설치 화면 자동 실행.
- 서명 키 `signing/release.keystore`는 자체 업데이트(동일 서명)를 위해 저장소에 포함. **삭제/교체 금지** (교체 시 기존 설치 위 업데이트 불가).

## 4. 데이터 모델 (`RaceRecord`)

| 필드 | 설명 |
|---|---|
| `title`, `type`, `dateEpochDay` | 대회 이름(연도 자동 제거) / 종목 / 날짜 |
| `subEvent` | 마라톤 세부종목(10K·하프·32K·풀코스) — **PB 판정 기준** |
| `recordTime`, `distance`, `startTime` | 완주 기록(HH:MM:SS) / 실제 거리(기록용) / 시작 시간 |
| `imagePaths`, `mainImageIndex`, `bgImageIndex` | 사진 경로들 / 대표 / 배경 이미지 인덱스 |
| `location`, `weather` | 장소 / 대회 시간대 날씨 요약 |
| `bodyInfo`, `bodyDateEpochDay` | 몸 상태 / 측정일(대회 -N일 표기) |
| `entryFee`, `eventFee`, `eventNote` | 기본 참가비 / 이벤트 추가금 / 이벤트 구성 |
| `bib`, `memo`, `ocrText` | 배번 / 소감 메모 / OCR 원문 |
| `stravaInfo`, `routePolyline` | Strava 지표 요약 / 코스 폴리라인(다중은 줄바꿈 구분) |

## 5. 외부 서비스 의존

| 서비스 | 용도 | 키 |
|---|---|---|
| GitHub Releases | 배포·자체 업데이트 | 불필요 |
| Open-Meteo | 날씨(과거/예보) | 불필요 |
| Nominatim(OSM) | 지오코딩 | 불필요 |
| OpenStreetMap 타일 | 코스 지도 배경 | 불필요 |
| DuckDuckGo 이미지 | 예정 대회 웹 사진 검색 (비공식) | 불필요 |
| ML Kit OCR | 기록증 텍스트 (온디바이스) | 불필요 |
| Health Connect | 몸 상태 읽기 | 권한만 |
| Google Drive API | 자동 동기화 | GCP OAuth (등록됨) |
| Strava API | 활동 가져오기 | Client ID 264239 + Secret(GitHub Secret `STRAVASECRET`) |

## 6. 알려진 제약

- Strava에서 몸 상태(체중 등)는 못 가져옴 — 헬스커넥트 경로 사용.
- InBody/가민 값은 각 앱이 **헬스커넥트로 내보내야** 보임. 그래프의 과거 범위도 각 앱이 실제로 넣어둔 만큼만.
- Drive 동기화는 last-writer-wins — 두 기기에서 동시에 편집하면 한쪽이 밀릴 수 있음.
- DuckDuckGo 이미지 검색은 비공식 엔드포인트라 언제든 동작이 바뀔 수 있음(실패 시 빈 결과).
- 미래 대회 날씨는 16일 이내 예보만 가능.
- 서명 키가 공개 저장소에 포함됨 — GitHub Secrets로 이전 권장(자체 업데이트 유지 조건).
